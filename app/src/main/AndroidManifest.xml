<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yfoughali.foot">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_foot"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_foot_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".L1Activity" />
        <activity android:name=".SAActivity" />
        <activity android:name=".LIGAActivity" />
        <activity android:name=".BLActivity" />
        <activity android:name=".ClassementL1Activity"></activity>
    </application>

</manifest>